package com.sudokuengine.app

import kotlin.test.Test
import kotlin.test.assertEquals

/**
 * Trivial test so the unit-test task and Kover have something to run/measure for this
 * scaffold. Replaced by real pipeline/UI tests in follow-up PRs (see `docs/APP_SPEC.md`).
 */
class PlaceholderTest {
    @Test
    fun `scaffold sanity check`() {
        assertEquals(2, 1 + 1)
    }
}
