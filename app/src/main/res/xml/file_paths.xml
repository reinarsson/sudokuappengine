<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Camera capture writes here in a future PR (TakePicture + FileProvider). -->
    <cache-path name="captured_images" path="images/" />
</paths>
