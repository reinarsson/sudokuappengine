root = true

[*.{kt,kts}]
# Jetpack Compose convention: @Composable functions are PascalCase (they behave like
# components/types from the caller's perspective). Allow this for ktlint's
# function-naming rule.
ktlint_function_naming_ignore_when_annotated_with = Composable
