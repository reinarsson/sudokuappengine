# Default — normalize all text files to LF
* text=auto eol=lf

# Windows scripts keep CRLF
*.bat text eol=crlf

# Binary assets — never normalize line endings (would corrupt the model / fixtures)
*.tflite binary
*.png binary
